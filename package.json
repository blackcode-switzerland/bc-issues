{
  "name": "blackcode-platform",
  "version": "1.0.0",
  "description": "Blackcode internal app platform — monorepo. Apps live in apps/*, shared libraries in packages/*.",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=issues",
    "build": "turbo run build",
    "start": "turbo run start --filter=issues",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "db:generate": "npm run db:generate --workspace=issues",
    "db:migrate": "npm run db:migrate --workspace=issues",
    "db:push": "npm run db:push --workspace=issues",
    "db:studio": "npm run db:studio --workspace=issues"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "engines": {
    "node": ">=20"
  }
}
